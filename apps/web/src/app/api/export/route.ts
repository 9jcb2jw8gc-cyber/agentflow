import { NextResponse } from "next/server";

export async function POST() {
  // TODO: implement canvas → YAML export
  return NextResponse.json({ message: "Export endpoint — not yet implemented" });
}
